#!/bin/bash
 
# if [ $OSTYPE = msys ]; then
#     # SSH_PID=$(ps -ef | grep 'ssh-agent' | awk '{print $2}')
#     if [[ -z $SSH_AGENT_PID ]]; then
#         cd ~
#         eval $(ssh-agent -s)
#         ssh-add .ssh/id_rsa
#     fi
# fi

# [[ -f "/usr/bin/xcompmgr" ]] && xcompmgr -cF &
# [[ -f "/usr/bin/nabi" ]] && nabi &
# ~/mymount.sh
